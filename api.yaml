apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: api-cpu
  name: api-cpu
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: api-cpu
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.network/odfe-net: "true"
        io.kompose.service: api-cpu
    spec:
      containers:
        - image: phucnh22/mapintel-api-cpu
          name: api-cpu
          ports:
            - containerPort: 8000
          resources: {}
          volumeMounts:
            - mountPath: /home/user/artifacts
              name: api-cpu-data
      restartPolicy: Always
      volumes:
        - name: api-cpu-data
          persistentVolumeClaim:
            claimName: api-cpu-data

status: {}

---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: api-cpu
  name: api-cpu
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
  selector:
    io.kompose.service: api-cpu
status:
  loadBalancer: {}
